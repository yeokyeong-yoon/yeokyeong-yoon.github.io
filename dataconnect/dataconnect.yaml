specVersion: "v1beta"
serviceId: "github"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "github-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
